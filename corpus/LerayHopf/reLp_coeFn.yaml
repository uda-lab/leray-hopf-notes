name: LerayHopf.reLp_coeFn
tier: full
statement_ja: |
  実部写像の値の点別表示：$\mathrm{Re}$ を取った $L^2$ 類の代表関数は a.e. で点別実部に一致する。
proof_ja: |
  構成の展開そのものである。
gap:
  level: none
  note: |
    lean-pde PR #126 で `Analysis/RealComplexLpBridge.lean` へ公開移動（private→public）。
chapter: energy
tags: [real-complex-bridge, shared-infra]
