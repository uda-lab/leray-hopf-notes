name: LerayHopf.gradVF_coeFn
tier: gloss
statement_ja: |
  上のベクトル場の、ほとんど至るところでの値。各点で、方向ごとの偏微分を係数とする基底ベクトルの和になる。
gap:
  level: none
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第8波（コンパクト性エンジン 4 モジュール: `SpatialCompactness` / `SpacetimePrecompact` / `ArzelaAscoliTime` / `Bochner.StepFunctionCompactness`）で手書き化。抽出元: `LerayHopf/R3/SpatialCompactness.lean` の `LerayHopf.gradVF_coeFn`。
chapter: compactness
tags: [private-helper, compactness, divergence-free]
