name: LerayHopf.Bochner.timeMollification_of_w1pTime
tier: full
statement_ja: |
  時間軟化データの選択：`W1pTime` 元から時間軟化データを（存在定理の代表元選択で）供給する。
proof_ja: |
  [[存在定理|LerayHopf.Bochner.timeMollification_exists]] の非空性から選択で代表を取るだけである。
gap:
  level: large
  note: |
    依存先の [[存在定理|LerayHopf.Bochner.timeMollification_exists]] が現状 `sorry` を含むため、この選択の値も同じ壁に条件づけられている（それを隔離して後続の定理を記述できるようにするため）。
provenance: |
  notes#146: `sorry` を持つ `timeMollification_exists` への依存は `gap.note` に記載されていたが、機械可読な完了状態の印が無かったため site 上で verified として表示されていた。印を付けた。
chapter: bochner
tags: [gelfand-triple, bochner]
proof_status: contains-sorry
