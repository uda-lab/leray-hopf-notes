name: LerayHopf.abs_cutoff_le_one
tier: gloss
statement_ja: |
  `cutoff_nonneg` と `cutoff_le_one` を合わせた $|\mathrm{cutoff}\ R\ x|\le 1$。打ち切り後の関数の一様有界性を導く際に使う。
gap:
  level: mild
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第1波（SobolevEmbedding.lean クラスタ）で手書き化。抽出元: `LerayHopf/R3/SobolevEmbedding.lean`。
chapter: compactness
tags: [private-helper, sobolev-embedding]
