name: LerayHopf.Torus.galerkinODE_functional_apply
tier: full
statement_ja: |
  上記汎関数の値の展開規則：$w$ での値は $-\nu\, s(u, w) - b(u, u, w)$ に等しい。
proof_ja: |
  構成の書き下しそのものである。
gap:
  level: none
  note: |
    `rfl` の一行で、`@[simp]` 補題として登録されている。
chapter: ode
tags: [galerkin, ode, torus]
