name: LerayHopf.continuous_restrictToBall'
file: LerayHopf/R3/ArzelaAscoliTime.lean
tier: gloss
statement_ja: |
  半径を固定した球への制限は連続である。差について $1$-Lipschitz であることから直ちに従う。
gap:
  level: none
  note: |
    同名の宣言が `LerayHopf/R3/SpacetimePrecompact.lean` にも別に存在する（そちらの docstring は `LerayHopf.R3.AubinLionsLimitPassage` の `private` 版の複製と述べている）。名前が衝突するため corpus 側は whole-slug のファイル名と `file` フィールドで区別している。
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第8波（コンパクト性エンジン 4 モジュール: `SpatialCompactness` / `SpacetimePrecompact` / `ArzelaAscoliTime` / `Bochner.StepFunctionCompactness`）で手書き化。抽出元: `LerayHopf/R3/ArzelaAscoliTime.lean` の `LerayHopf.continuous_restrictToBall'`。
chapter: compactness
tags: [private-helper, compactness, spatial-compactness]
