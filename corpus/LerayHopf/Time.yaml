name: LerayHopf.Time
tier: full
statement_ja: |
  時間軸：実数直線 $\mathbb{R}$。
proof_ja: |
  実数直線の略記として定める。
gap:
  level: none
  note: |
    型シノニム（`abbrev`）。汎用 Galerkin 層の解曲線 `Time → ↥D.σ` の定義域など、時間変数の意図を型面で明示するために使われる。
chapter: spaces
tags: [foundations]
