name: LerayHopf.measurable_natFloor_real
file: LerayHopf/R3/SpacetimePrecompact.lean
tier: gloss
statement_ja: |
  実数に対する床函数は可測である。値域が可算なので、各値の逆像が区間として可測であることを見ればよい。
gap:
  level: none
  note: |
    `LerayHopf/Bochner/StepFunctionCompactness.lean` の同名の `private` 宣言の複製である。`private` は import 境界を越えて見えないため、こちらのファイル内に置き直されている。名前が衝突するため corpus 側は whole-slug のファイル名と `file` フィールドで区別している。
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第8波（コンパクト性エンジン 4 モジュール: `SpatialCompactness` / `SpacetimePrecompact` / `ArzelaAscoliTime` / `Bochner.StepFunctionCompactness`）で手書き化。抽出元: `LerayHopf/R3/SpacetimePrecompact.lean` の `LerayHopf.measurable_natFloor_real`。
chapter: compactness
tags: [private-helper, compactness, bochner]
