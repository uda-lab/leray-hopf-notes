name: LerayHopf.stokes_boxSum
tier: gloss
statement_ja: |
  帯域制限されたテスト場に対して、Stokes 双線形形式が Fourier 箱の上の有限和に落ちること。テスト場の Fourier 係数が箱の外で消えることによる。
gap:
  level: none
  note: |
    `LerayHopf/Torus/LimitPassage.lean` の `private` な `stokesTestPairing_eq_boxSum` と主張・証明ともに同一の複製である（`private` は import 境界を越えて見えない）。名前だけが異なる。
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第9波（Torus 極限移行チェーン: `ConvectionExtension` → `LimitPassage` → `TraceEnergy` → `ViscousLimit`）で手書き化。抽出元: `LerayHopf/Torus/TraceEnergy.lean` の `LerayHopf.stokes_boxSum`。
chapter: energy
tags: [private-helper, torus, limit-passage]
