name: LerayHopf.truncated_hasCompactSupport
tier: gloss
statement_ja: |
  $R>0$ のとき、打ち切り Schwartz 関数 `cutoff R • φ` が閉球 $\overline{B(0,2R)}$ にコンパクト台をもつこと。`truncated_contDiff` の $C^1$ 性と合わせて `gns_L6_of_memH1_R3` の Schwartz 近似列（A2 段階）を与える。
gap:
  level: mild
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第1波（SobolevEmbedding.lean クラスタ）で手書き化。抽出元: `LerayHopf/R3/SobolevEmbedding.lean`。
chapter: compactness
tags: [private-helper, sobolev-embedding]
