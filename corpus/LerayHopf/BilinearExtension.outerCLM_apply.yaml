name: LerayHopf.BilinearExtension.outerCLM_apply
tier: gloss
statement_ja: |
  外側拡張の値の整合性（$D$ 上で内側拡張に一致）。
gap:
  level: none
provenance: |
  lean-pde PR #123（issue #111 PR-4）で両系統の BLT tower（`convBLT*` 一族）を一本化した汎用稠密拡張モジュールの部品。
chapter: misc
tags: [bilinear-extension, shared-infra]
