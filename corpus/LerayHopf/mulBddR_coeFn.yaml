name: LerayHopf.mulBddR_coeFn
tier: gloss
statement_ja: |
  [[点ごとの有界乗算|LerayHopf.mulBddR]]の値は、ほとんど至るところで通常の点ごとの積 $h(x)\,a(x)$ に一致する。
gap:
  level: none
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第5波（`ConvectionForm.lean` クラスタ）で手書き化。抽出元: `LerayHopf/R3/ConvectionForm.lean` の `LerayHopf.mulBddR_coeFn`。
chapter: limit-passage
tags: [private-helper, convection-form, l2-multiplier]
