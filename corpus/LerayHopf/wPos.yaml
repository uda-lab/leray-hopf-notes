name: LerayHopf.wPos
tier: gloss
statement_ja: |
  Bessel 型の重み `wPos`：$\xi\mapsto(1+\lVert\xi\rVert^2)^{1/2}$（次数 $1$）。重み付き Fourier 特徴づけ（A3/A4 段階）で $H^1$ ノルムを Fourier 側の重み付き $L^2$ ノルムへ変換するために使う。
gap:
  level: mild
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第1波（SobolevEmbedding.lean クラスタ）で手書き化。抽出元: `LerayHopf/R3/SobolevEmbedding.lean`。
chapter: compactness
tags: [private-helper, sobolev-embedding]
