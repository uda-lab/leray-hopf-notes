name: LerayHopf.abs_mLD_le
tier: gloss
statement_ja: |
  $\sigma_m$ の絶対値が $2\pi\lVert m\rVert$ 以下であること（Cauchy–Schwarz 不等式と、次の評価から。これは $|w_-|\le1$ だけでは足りない、より強い評価である）：

  $$\lVert\xi\rVert\, w_-(\xi)\le1$$
gap:
  level: mild
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第1波（SobolevEmbedding.lean クラスタ）で手書き化。抽出元: `LerayHopf/R3/SobolevEmbedding.lean`。
chapter: compactness
tags: [private-helper, sobolev-embedding]
