name: LerayHopf.abs_mLD_le
tier: gloss
statement_ja: |
  `mLD` の絶対値が $2\pi\lVert m\rVert$ 以下であること（Cauchy–Schwarz 不等式と `wInv` の有界性 `abs_wInv_le_one` から）。乗数の一様有界性。
gap:
  level: mild
provenance: |
  Wave 4 の private gloss 機械生成テンプレートから、issue #74 第1波（SobolevEmbedding.lean クラスタ）で手書き化。抽出元: `LerayHopf/R3/SobolevEmbedding.lean`。
chapter: compactness
tags: [private-helper, sobolev-embedding]
