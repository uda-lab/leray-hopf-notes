# notes#73: per-declaration pages are prerendered at real paths (site/decl/<segment>/),
# so a sitemap now has something to list: independently fetchable, indexable documents
# rather than fragments of one page. The hash routes (`#/decl/<slug>`) still work and are
# unchanged — the static pages are additive (owner decision 2026-08-03) — but they are not
# listed here, because a crawler never receives a fragment.
User-agent: *
Allow: /

Sitemap: https://uda-lab.github.io/leray-hopf-notes/sitemap.xml
